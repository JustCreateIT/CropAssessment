$(document).ready(function(){
	/*
	$('input').on('click touchend', function(e) {
		var el = $(this.parent);		
		//var link = el.attr('href');
		var post_to = el.attr('action');		
		$.post( post_to );
	});   
	
	$('input').on('touchend click', function(event) {
		event.stopPropagation();
		event.preventDefault();
		// this fires once on all devices

		var el = $(this.parent);		
		//var link = el.attr('href');
		var post_to = el.attr('action');		
		$.post( post_to );
	});
	*/ 
});
